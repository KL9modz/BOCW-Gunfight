# TEST B9 — does a script-driven map_restart LINK a newly injected payload?
#
# The last gate on unattended operation. Carries a VERSION stamp; two builds
# differ by that one line, so the number on screen says which build the VM linked.
#
# ⚠ WRITES: calls map_restart(). Once-guard with two carriers, same as B4.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
