# TEST C7 — addtestclient. Measures the REAL client ceiling by filling the lobby
# until the engine stops accepting clients, rather than reading com_maxclients
# and trusting it.
#
# ⚠ THIS WRITES. It adds clients to a live session. Separate project on purpose:
# an injected payload that can only do one thing cannot accidentally do another.
# Run it alone, and test a lobby return afterwards.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
