# TEST P7 — can the lobby display anything a GSC payload prints?
#
# ⚠ FRONTEND HOOK (load_shared.gsc) — P1's proven hook, where the server VM
# demonstrably runs in the lobby (probe 51 = 63).
#
# Read-only apart from text on screen. Tries three type=0 print surfaces and
# gives each a distinct number, so whichever appears identifies the call.
#
# ⚠ print() and println() are deliberately NOT tried: both are type=1, the same
# dev flag as the file I/O family that crashed the game on 2026-09-10.

game=T9
mode=mp
script=scripts\core_common\load_shared.gsc
