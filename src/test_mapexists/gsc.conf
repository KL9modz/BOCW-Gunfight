# TEST B5 — mapexists() over all 38 map names from the dump's scripts/mp/.
#
# ⚠ READ-ONLY. Asks the engine which names this install can load. Loads nothing,
# tears nothing down.
#
# Exists because A4 did the opposite on 2026-09-08: map("mp_hijacked_rm") began
# tearing down the live map, never brought up the target, and left the session
# glitched on the old map with no error. That failure is silent and destructive,
# so the name must be checked BEFORE the teardown.
#
# ⚠ READ PROBE 9 (the control) FIRST. 900002 = trustworthy. Anything else means
# mapexists is useless here and the bitmasks are noise, not data.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
