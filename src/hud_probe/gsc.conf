# HUD probe — every on-screen display channel the menu does not use yet, one stage each,
# so one launch answers "does it draw in an MP match, where, and does a joiner see it".
# Server GSC only, no client payload, no memory write. docs/notes/hud-channels.md holds
# the evidence (file:line for every stage) and the RECORD SHEET to fill in.
#
# Same hook as mp_probe / lui_probe / gunfight_menu. Inject INSTEAD of the menu for this
# launch (one payload per launch, shared replace target): bash tools/inject.sh hud_probe

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
