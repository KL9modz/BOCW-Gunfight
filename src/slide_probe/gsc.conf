# slide_probe — measures every slide and tries the script-side slide-distance levers.
# Same hook as hello_world / mp_probe. ONE payload per launch: runs INSTEAD of gunfight_menu.
# docs/notes/slide.md has the engine model this probe tests. Output = ONE feed line, every 2 s.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
