# TEST L8 — does com_maxclients keep following maxplayers? The 6v6 question.
#
# ⚠ WRITES a gametype setting, the same class gunfight.gsc:104 performs on
# Gunfight itself. Reverts by not injecting. Not a session write.
#
# Reads maxplayers AND com_maxclients together every round, which is the pairing
# L7b currently infers rather than observes.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
