# TEST A4 — script-driven map carry. Replaces the Atian Menu step in the hosting
# workflow with one builtin call, per atian-menu-source.md:19 (func_set_map ->
# map( map_name )).
#
# ⚠ THIS WRITES — it loads a map. The failure mode is a MAP LOAD LOOP, so the
# script carries three independent guards (read_only, already-there, and a
# game.-scope attempt counter capped at 1). Ships read_only = 1: run that first.
#
# Motivation: both DLL automation routes closed by measurement 2026-09-08 (D10
# dcfuncscw returns an empty table; D11 the lobby exports are absent from the
# binary). This reaches the same goal with no DLL at all.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
