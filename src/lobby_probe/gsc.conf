# Lobby probe — read-only. Enumerates gametype-string validity and the several
# distinct player-count builtins, none of which this project had been using.
#
# Builtins come from ate47/t8-atian-menu docs/notes/funcs_cw.csv, the author's
# Cold War function table (addresses in BlackOpsColdWar.exe). See
# docs/notes/cw-builtins.md.
#
# WRITES NOTHING. No addtestclient, no setteam, no switchmap. Those are real
# state changes and belong in their own staged test, not in a probe.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
