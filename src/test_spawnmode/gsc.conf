# TEST B8 — fix Gunfight's spawns above 3 per side.
#
# klaze has ALREADY had 4 on a Gunfight team via a lobby glitch he cannot
# reproduce, and reports it "causes other glitches such as incorrect spawns" -
# while noting Gunfight maps run Face Off 6v6 with proper spawns.
#
# That observation is the whole diagnosis. Only FOUR gametypes pin
# level.alwaysusestartspawns = 1: prop, sd, gunfight, vip. The global default is 0
# (globallogic.gsc:5146) and Face Off is not in the list. Same map, same spawn
# entities, 12 players, fine - because Face Off never pins the flag.
#
# So the spawn breakage is Gunfight's own setting, not a property of the map.
#
# ⚠ THIS WRITES level state and changes where everyone spawns. Run alone.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
