# Vehicle PARITY probe — read-only. Same hook as hello_world, mp_probe and gunfight_mod.
# Answers one question: do the model# names we have overlap the vehicle# names we need?
# 202 candidates (105 xmodel names, 68 campaign vehicle hashes, 29 behaviour-type strings)
# through isassetloaded(), emitted as counts. No spawnvehicle, no usevehicle, no state writes.
# Also reads the veh_spawn_point struct count, which tests whether stock Path A
# (mp_common/vehicle.gsc, entry point initvehiclemap() — zero callers in the dump) is
# reachable on this map at all.
# See scripts/vehicle_probe.gsc for the tagged-number output format and docs/notes/vehicles.md
# for why guessing is free.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
