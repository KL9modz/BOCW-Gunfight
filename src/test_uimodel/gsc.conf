# TEST P6a — enumerate the lobby's UI MODEL tree. READ-ONLY.
#
# ⚠ FRONTEND HOOK (load_shared.gsc). The LUI channel is the last route to the
# pregame map that has never been examined, and LUI owns the map picker.
#
# The chain is stock: cp_common/load.gsc:398 does
#   getuimodel( function_5f72e972( #"lobby_root" ), "transitionMapIdOverride" )
# and function_5f72e972 is a builtin (+9715b00) in BOTH the GSC and CSC tables.
#
# ⚠ READ PROBE 80 (the control) FIRST. 7 = three known models resolve and an
# invented one does not. Anything else and the bitmasks are noise - that is the
# B5 lesson, where mapexists() returned true for every name including fakes.

game=T9
mode=mp
script=scripts\core_common\load_shared.gsc
