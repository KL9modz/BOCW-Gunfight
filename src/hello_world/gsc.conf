# Hello-world validator for the MP injection path.
# Confirms the injector's documented MP hook fires in a CUSTOM GUNFIGHT lobby.
# See docs/notes/mp-load-path.md and docs/notes/pipeline-toolchain-survey.md.

game=T9
# MP VM (not ZM — every public tutorial primes through Zombies; this is the MP path)
mode=mp
# The injector HOOKS this stock script (does not overwrite it); when bb.gsc runs in
# the MP VM, our linked script's autoexec runs too. Documented MP hook point.
script=scripts\mp_common\bb.gsc
