# TEST B2 — set the map FROM THE PREGAME LOBBY with switchmap_load( map, gametype ).
#
# ⚠ FRONTEND HOOK. load_shared.gsc links in every VM, so this runs in the lobby
# as well as in the match. Only possible because P1 measured that a server VM
# executes in the frontend. tools/inject.sh knows this project.
#
# ⚠ WRITES: it loads a map. Ships read_only = 1 — run that first. Guards are a
# once-guard on two carriers and a target map known-good by history (mapexists()
# cannot screen it; B5 proved it returns true for everything).
#
# ⚠ No endon anywhere in the switch path: A4 measured the load firing
# #"game_ended" mid-sequence, which killed the thread before the commit.

game=T9
mode=mp
script=scripts\core_common\load_shared.gsc
