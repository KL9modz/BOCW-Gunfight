# Minimal client payload: proves whether an injected .csc runs here, and whether
# the replace target is safe. Calls NOTHING but a tick counter and a print.
#
# Exists to disambiguate a crash that had two candidate causes - an unsafe
# replace target, or fatal UI-model calls. Removing the model calls makes the
# result unambiguous either way.

game=T9
mode=mp
script=scripts\core_common\load_shared.csc
