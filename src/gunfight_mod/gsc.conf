# BOCW Gunfight mod — injector config.
# Same MP hook as the hello-world. Do NOT inject this until the hello-world has
# confirmed the hook fires in a custom Gunfight lobby. See src/README.md.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
