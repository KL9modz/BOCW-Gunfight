# TEST C10 — late joiners land on a TEAM instead of in spectator.
#
# klaze's hosting workflow, measured 2026-09-09: a 3v3 Gunfight pregame lobby holds
# 3 + 3 + 2 CoD Casters = 8, which is exactly com_maxclients. Extra players cannot be
# assigned, so they LEAVE the pregame lobby, the host starts, and they REJOIN once the
# match is running - and the game accepts them, as spectators.
#
# team_assignment.gsc:600-656 function_a3e209ba is the rule that sends them there, and
# it is a chain of AND conditions with two overridable links. Break either one and the
# same rejoin lands on a team instead, through an auto-assign path with NO per-team cap.
#
# So this test does not invent a mechanism. It removes one condition from a workflow
# klaze already performs, and the workflow does the rest.
#
# ⚠ THIS WRITES level state. Run alone, one match. Test a lobby return afterwards.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
