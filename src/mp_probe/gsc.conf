# MP probe — read-only diagnostics. Same hook as hello_world and gunfight_mod.
# Requires hello_world to have confirmed the hook first (done 2026-09-07).
# Reads com_maxclients, the timelimit setting and its clamp, the gunfight_zone_center
# count for the loaded map, the on_start cadence, and the live player count.
# See scripts/mp_probe.gsc for the tagged-number output format.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
