# GUNFIGHT HOST MENU - the in-match control surface (roadmap Goal A, item A1).
#
# One payload. It CONTAINS gunfight_mod's four verified fixes plus team size, and adds
# the menu on top, because B9 measured that a second payload cannot be injected under a
# live one - every injection uses the same replace target and clobbers the last. So
# everything the host needs has to ship in this one file, injected once per game launch.
#
# Same MP hook as gunfight_mod. gunfight_mod stays as the no-menu variant.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
