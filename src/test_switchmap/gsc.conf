# TEST C6 — switchmap_load. THE team-size question.
#
# Start in a private TDM lobby (12 slots), switch the GAMETYPE to Gunfight in
# place, and see whether com_maxclients follows the gametype or stays with the
# lobby. If it stays at 12, larger-than-3v3 Gunfight is script-reachable.
#
# ⚠ THIS RELOADS THE SESSION. Run alone. Test a lobby return afterwards.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
