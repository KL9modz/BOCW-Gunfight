# TEST C8 — setteam. Is 8 clients actually 4v4?
#
# Two phases, gated by a config switch in the script:
#   read_only = 1  team representation + distribution. WRITES NOTHING.
#   read_only = 0  also attempts to move one client onto another team.
#
# ⚠ Run with read_only = 1 FIRST. Phase 1 is what settles the argument shape,
# and it is free. Separate project from test_addclients on purpose: a payload
# that can only do one thing cannot accidentally do another.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
