# TEST C8 — the PER-TEAM ceiling. Can four players stand on one team?
#
# Replaces the earlier test_setteam, which was built on a wrong premise: setteam
# is an ENTITY function (55 stock call sites, all grenades/vehicles/turrets/pods),
# not a player team-assignment function. See docs/notes/dump-cross-check.md.
#
# ⚠ THIS WRITES. Adds bots to a named team. Run alone; lobby return afterwards.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
