# TEST L6 — maxplayers. Writes the gametype setting that lobby_probe measured at
# 2x the per-side team size, then reads it back on every round start to see
# whether the round boundary honours it or reverts it.
#
# ⚠ THIS WRITES, but only a gametype setting — the same class of write
# gunfight.gsc:104 and :106 already perform on Gunfight itself, and the same class
# as B6/B7. It reverts by not injecting. It is NOT a session write like C7.
#
# Ships with read_only = 0 and fill_bots = 0: write the setting, add nobody.
# Run that first. fill_bots = 1 is the second run, once the write is known to land.

game=T9
mode=mp
script=scripts\mp_common\bb.gsc
