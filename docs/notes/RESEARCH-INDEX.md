# Research index — the map-lobby problem & mod expansion (as of 2026-09-11)

Navigational map of the notes. Start here.

## The goal
Real **Gunfight** gametype on **any map**, at the **lobby/match/state level**, **no glitch steps**, with
**friends able to join and play** (vanilla, nothing installed). Private matches only, throwaway box.

## Current state of the map problem — ONE live path; the two below it closed on 2026-09-11
0. ▶▶ **`LobbySetMap` / `LobbySetGameType`** (NEW 2026-09-12, untested, tool written) — **the layer the
   whole hunt skipped.** Every closed route attacks the *compat set* (make the picker allow the pair);
   this one skips the picker and calls the two engine functions the picker itself calls. ACTS names both
   with BO4 addresses **0x10 apart**, and ate47's Cold War port **ships disabled with two bugs**;
   `tools/lobby-set.py` is it finished, scan-only by default. ⚠ Two honest risks, both from our own
   measurements: the Lua master may re-push the selection, and `CreateRemoteThread` is one API beyond
   gfscan and `injectcw`. → `lobby-setters.md`
1. ✅ **`switchmap_load` in-match** — 🪦 ~~CLOSED 2026-09-11: inert in MP~~ **RETRACTED 2026-09-12**:
   the "inert" runs had their map-name literal garbled by the ACTS string header. Stripped
   (`tools/strip-strhdr.ps1`), `switchmap_load` + `switchmap_switch` **moves the session** (lobby
   follows, 12 slots read in a Gunfight session), and **`switchmap_load` alone stages the pair as the
   lobby's next map** — the match ends and the pregame lobby has it selected (klaze, 2026-09-12).
   Both are menu verbs (*Stage for lobby* / *Switch NOW*). ⚠ Joiners still untested on both.
   → `session-switch.md`
2. 🪦 ~~**Cheat Engine on the client-LUI compat state**~~ — **CLOSED 2026-09-11**: TAC will not let CE
   run at all, and a custom watchpoint tool hits the same anti-debug wall.

### Closed by measurement (don't reopen without new evidence) — see `game-systems.md §4/§8/§10`
carry (crashes joiners) · GSC/UI-model from any injectable VM (P1–P11) · client-frontend VM (uninjectable) ·
memory value-scan of C compat state (Lua-internal) · save edit (server-side) · no map-unlock dvar ·
`map_restart` (same map) · `forcegamemodemappings` (campaign-only) · TDM reskin (ruled out by requirement) ·
manual/automated glitch (ruled out by requirement).

## Key docs
| Doc | What's in it |
|---|---|
| `pregame-routes.md` | the full map-hunt log: P1–P11, memory scans, the glitch tutorial + mechanism, the switchmap PRIORITY AVENUE, the requirement pin |
| `game-systems.md` | **how the game works / mod-expansion reference** (20 sections): Gunfight anatomy; clientfield sync (+joiner-reach correction); builtins; events; injection surface (10); persistence/save (13); scoring/round-flow (14); loadout struct/perks/content refs (15,17,20); spawning + the combined-arms spawn bug + #spawn_guard (16,16b); CSC match VM (12); host-migration (18); bots (19); expansion roadmap (9) |
| `spawn-system.md` | **the spawn picture (2026-09-14)**: CW folds every mode's markers into one flagged `mp_spawn_point` family (no `gunfight` flag exists); Gunfight = TDM's start spawning, always; the four spawn paths (override → start → scored → map centre) and which symptom each produces; the three maps that switch layout by gametype (bounds only); five usable mechanisms (S&D-family borrow, the pre-spawn override, …) and the probe rows that measure a map |
| `mode-remnants.md` | **the lobby-state model (2026-09-14): session descriptor vs. lobby mode config**, why Case A locks the picker and Case B says TDM, the async-load fact behind Stage (wait for STAGE READY), the 25 s explained (`gf_switch_wait`), and the A/B/T settings-census run that yields the Gunfight profile |
| `lobby-setters.md` | **the one live map route**: call `LobbySetMap`/`LobbySetGameType` directly, skipping the compat gate rather than beating it. Why it is not in the closure table, the two ways it could still fail, and `tools/lobby-set.py` |
| `desktop-session.md` | ▶ **the runbook** — everything open, ordered by what it costs, stages 0–7. Rewritten 2026-09-12 against the measured state: stage 3 is the save-based hosting workflow (P5's follow-through, mostly needs no injector), stage 6 lists what is closed so it is not redone |
| `overtime-zone.md` | **the overtime capture zone on any map** (`#synth_zones`, built 2026-09-12, UNTESTED, `gf_zone` default off): hand stock `setupzones()` a centre + trigger anchored on Domination's neutral B flag, with the ordering proof, the `gettimelimit` extra-time trap, the abort_level valve and a read-only census. The menu's `Overtime zone` page |
| `switchmap-test-protocol.md` | staged run sheet for the switchmap test payload (🪦 avenue closed) |
| `atian-menu-source.md` | the carry menu internals; the two map-change paths |
| `cw-builtins.md` | the engine builtins with addresses (isvalidgametype, switchmap_*, sessionmode*, etc.) |
| `../reference/bocw-maps.md` | **every loadable MP map, display name ↔ map name, audited 2026-09-12** (dump map table + wiki `console` field + asset fingerprints). The Strike/12v12 split is a *gametype-string* branch inside one map file, not a second map; Nuketown Holiday/Halloween are two unresolved dvars. The menu's labels were regenerated from it |
| `../reference/bocw-weapons.md` | **every MP loadout weapon name (64) with display names + confidence, 2026-09-13** (gun-levels CSV ∩ custom-games DDL enum). Menu Weapons page is generated from it. ⚠ No rock/gulag/throwable weapon exists in T9 — the Gulag rock is an IW8 (MW/Warzone) asset; 19 hashed enum ids brute-forced, no hit |
| `lui-events.md` | **a 4th display channel, seen in-game 2026-09-13:** `luinotifyevent( #"<stock event>", n, ... )` makes the client's shipped LUI draw its widget — the Pause's TIMEOUT overlay is `esports_game_paused`, the countdown is `create_prematch_timer`. Stock widgets + stock (hashed) strings, numbers free; ~40 named events catalogued |
| `lui-elems.md` | **positioned text rows from script — MEASURED 2026-09-13:** the luielem system (`LUIelemText`/`LUIelemImage`) works in MP from an injected CLIENT payload (`src/gunfight_menu_c/`): the element rendered a stock key; a plain string in it was a **fatal LUI error** (`localizeentry`, second field = hash of the string). Stock localized keys only, a wrong key crashes the client — a status panel, not a free-text menu; the hint panel remains the free-text ceiling. Crash-dump decode recipe |
| `hud-channels.md` | **every way script can put something on screen (2026-09-23, read from the dump, unrun):** the menu uses feed / centre / hint / 3 LUI events; the dump has ten more. Headline: **event-backed luielems** (`LUIelemBar`, `LUIelemCounter`) register NO clientfields and take no material, so a positioned box/bar and a positioned number from host GSC may reach joiners (stock MP drives the same transport for the Cruise Missile lock-on; open question = do the MP widgets ship). Ready now, all stock MP shapes: full-screen colour + drawHUD veil, lower message + countdown, announcement, the N-v-M banner, EMP overlay, objective progress rings / per-player markers, HUD switches. Free-text lead: `TempDialog` (stock feeds it runtime strings). The rules (one LUI event per client per frame, plain text only where stock feeds it). `src/hud_probe/` + record sheet |
| `fun-pack.md` | **the fun-menu gaps Project HiNAtyu's CW menu showed (2026-09-23, built, never run):** PHA V1.00's compiled menu disassembled (ACTS's VM38 opcode table, verified by construction) for its full tree and the builtins behind each feature, then rebuilt from stock shapes — fast restart row, fly bind (Tac + Melee), slide page (speed / long / super / chain), disco camo, prop disguise (Prop Hunt's recipe), forge tools (spin / bob / link / solid / tilt) + prop gun, shots per trigger, grenade swap, model cannon, 9 appended vehicle rows. 14-step test sheet; the list of PHA features still absent (aimbot / ESP held back pending a decision) |
| `crash-decode.md` | **crash reports -> the GSC line (tool + recipe, 2026-09-21):** `sre_stack` offsets are FILE offsets into the .gscc pointing at the call instruction of each frame, innermost first; the header crc is constant across our builds so a non-chaining decode means an older build was running (try payloads/*.bak.gscc). `tools/sre-decode.py <payload> <offsets>` names the frames. Three crashes decoded inside |
| `late-join.md` | **late joiners placed, not benched (built 2026-09-21, never run):** stock benches a mid-match human joiner (`player_connect.gsc:283` -> `menuautoassign` -> the `team_assignment.gsc:602` gate answers spectator, nothing revisits it). The mod hooks `level.autoassign` and hands stock a side as if the lobby had: fewer humans -> losing side -> tie: benched with a pause-menu CHANGE TEAM reminder (`gf_teamchange` writes the hidden `allowingameteamchange` setting). One bot leaves the joined side if it became the bigger one. `JOIN` host feed line measures what the lobby hands a late joiner. Test sheet inside |
| `client-control.md` | **per-client control (built 2026-09-15, never run):** the Players page gives the host a page per connected client - godmode, max ammo, give weapon / camo / operator / outfit (the shared hubs aimed at the client via a target context that auto-drops), fly, third person, speed, kill, take/strip weapons, freeze, team, kick - all server-side builtins on the player entity (joiner-safe, stock bot_devgui precedent); app parity via `*one` verbs + `cmd_hub_verb` |
| `destructibles.md` | **destructibles + radiant exploders (built 2026-09-17, never run):** the map's breakables enumerate with one `getentarray` and break with one `dodamage` (stock's own shape, joiner-safe visual); the radiant exploders ship per map as hashes the API takes as-is — `tools/exploders-gen.py` bakes 699 rows / 32 maps / 34 named into the menu as a walker. The `DESTRUCT` census line + `GFMAPDEST` channel recover the real destructible names the manifests hash. Test sheet inside |
| `projectiles.md` | **bullets into rockets (built 2026-09-17; 2026-09-20 klaze: never fired — §8 diagnostic build: PROJ counter line + 6 spawn methods incl. explosive rounds, `payloads/gunfight_menu.projdiag.gscc`):** `weapon_fired` → `magicbullet( w, eye, far, self )`, rate-gated (default 300 ms; "every shot" is the full-auto measurement), nine universal projectile weapons validated against `level.weaponnone`, homing via `missile_settarget`, optional trail FX. Tracers have no script API; beams are `.csc`-only. Test sheet inside |
| `static-props.md` | **props (built 2026-09-17, never run):** Prop Hunt's per-map `_ph.csv` table read at runtime + a 48-model universal set (the 12 `_prophunt` + `p9_*` scenery from the zone manifests), every row `isassetloaded( "xmodel" )`-gated (a valid type string, measured), placed where the host looks; Gate 2 does not apply. Test sheet inside |
| `teleport.md` | **teleport (built 2026-09-15, never run):** everyone / a side to me, to the crosshair, to a saved point or the map centre (a ring, each spot floored), me to any of those, per-player to-me / me-to-them / swap, and the classic **teleport gun** (one move per shot) + **teleport grenade** (appear at the detonation), host or everyone. The move is stock's own fast-travel idiom (`red_door.gsc:492`: dontinterpolate → setorigin → setvelocity 0 → setplayerangles), the floor is Prop Hunt's `playerphysicstrace` sweep, the hooks are the `weapon_fired` / `grenade_fire` notifies. Test sheet inside |
| `vehicle-mode.md` | **vehicle mode — everyone spawns riding (built 2026-09-17, never run):** klaze's "gunfight on bikes / in attack helicopters". Stock's Fireteam spawn-in-vehicle shape (`spawning_squad.gsc:1578`: `player.var_5a44792f = 1` + `usevehicle`) on every spawn, the ride = the first `isassetloaded` candidate of a class (bikes / Hind / care-package heli everywhere / snowmobiles / quads / tanks / cars / streak gunship / AUTO); "cannot get off" = the `disable_usability` value layer + a re-seat watcher (both unmeasured); ground rides braked through the countdown, air rides spawn at GO at height; vehicle death kills the rider (stock). `gf_vehmode` + options, app section, `VEHMODE` debug line. Test sheet inside |
| `map-data.md` | **per-map data (built 2026-09-15, untested):** the spawn keys the engine actually reads off `mp_spawn_point` structs (mode flag `.tdm`/`.sd`/…, team `.group_index` 1/2, START flag = field hash 0xa3c53936 alias `_human_were`, HQ `.ishqspawn`; hashed spawning scripts `script_335d0650ed05d36d.gsc:194` / `script_44b0b8420eabacad.gsc:306`) → **Family AUTO (default): the map's authored S&D starts, else its TDM starts**, a STARTS tally on the FAMILIES line; the **per-map vehicle list lives in the dump** (`tables/bgcache/<zone>.csv`, matched Standoff's census) → `tools/mapdata-extract.py` / `docs/data/map-assets.json` and a Vehicles page built per map from `isassetloaded`; the Prop Hunt table + vehicles + spawn keys are published to the app (`GFMAPVEH/PROP/SPAWN`, `tools/gf-control/mapdata_scan.py` → `mapdata/<map>.json`). Test sheet inside |
| `slide.md` | **slide distance (2026-09-20, engine read off the live exe):** the distance is a fixed DURATION (`T[0].ms × AE stat 0xc4`) × a START SPEED (`T[0].speed × weapon scale × (1−slide_subsequentslidescale)^n`, n = slides chained within 1.25 s), then friction — no speed/duration dvar exists (4,497 live dvars enumerated, every registration site mapped). Nothing re-clamps the speed after the start frame, so the script lever is a `setvelocity` scale at `slide_begin` (+ optional per-frame hold); `slide_subsequentslidescale` and the `specialty_mod_phdflopper` perk branch are the other two. `src/slide_probe/` measures all of it in one SLIDE line (built, never run). `tools/dvar-live/` = the read-only live-dvar / registration-map / xref / disasm kit |
| `gf-panel.md` | **the native control app (2026-09-20, C# .NET 9 WPF, `tools/gf-panel/`):** klaze's rcon-tool model rebuilt as a real Windows app, self-contained single-file exe (friends install nothing). Every menu page/sub-option as data-driven settings rows + the rcon-only features BOCW supports (everyone-state, end round / restart round / balance / next-match staging / session bans, vision · drunk · sound · give-all · timescale, a private-match rotation, config presets, bot profiles, the whole prop catalog + explosive barrels). Reads = one read-only memory sweep per 1.5 s for GFSTATE (new) / GFPLAYERS (new) / GFCFG / GFROSTER / GFMAP*; writes = the paced bridge with `gf_cmd_seq` acks the GSC echoes. GSC increment in `gunfight_menu.gsc` (state/players publishers, acks, say audience, `panel_verb`), side payload `gunfight_menu.panel.gscc` — all NEVER RUN. **§6 the overlay (2026-09-23):** the panel itself laid over the game on a hotkey (Insert; borderless, topmost only while it has the focus, put back on Esc / click-away) — no DLL, no hook; needs Fullscreen Borderless; never run on Windows, unknowns: `RIDEV_NOHOTKEYS`, TAC vs a topmost window |

## The glitch (for reference; ruled out as a method but documents the target state)
GlitchHunterz's tutorial (youtu.be/Wxctp-7rrEs, the founder; FacelessOne's youtu.be/uzXXE7v_PBU re-posts
the same text): a two-account menu race that carries an online playlist's mode object (full map-compat +
com_maxclients) into a private lobby. All three variants verbatim in `roadmap.md §B2` (2026-09-13);
mechanism in `pregame-routes.md`.

## The rest of the scene (swept 2026-09-13) — `ecosystem-survey.md`
No other open MP gametype/lobby project exists. The only new thing is the Zombies-only *T9 Mod Manager*
scene (Aug 2026), whose mods configure themselves in the ZM pregame lobby — the one group plausibly on the
same layer. Client route dead; ACTS 3.3.0 is the current pin.

## Mod expansion — grounded next features (`game-systems.md §9, §11`)
synth overtime zones on any map (script can spawn triggers) · custom/themed loadouts (shipped melee/snipers
bundles) · **loadout-pool camo** (`setcamo` per spawn — `loadout-camo.md`, built, untested) · **bots**
(add/remove one, even-up for an odd human count, per-team difficulty via the cracked
`bot_difficulty_<team>` setting, and a CUSTOM difficulty struct past the stock ceiling — `bots.md`, built,
untested) · **per-client control** (Players page: god / ammo / weapons / cosmetics / fly / speed / kill / kick per connected client — `client-control.md`, built 2026-09-15, untested) · expose stock settings · map control (pending the switchmap test) · all bounded by: joiners get
server-GSC gameplay + stock clientfields only (§2, §10).
